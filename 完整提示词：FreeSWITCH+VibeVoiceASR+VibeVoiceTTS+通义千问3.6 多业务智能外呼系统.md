# 完整提示词：FreeSWITCH\+VibeVoiceASR\+VibeVoiceTTS\+通义千问3\.6 多业务智能外呼系统

# 【完整官方提示词】可直接复制使用

下面是整理好的、无删减、连贯、工程级提示词，包含你的全部原始要求：架构、软件、业务、隔离、部署、安装、配置。

---

## 一、用户原始需求（完整保留）

请设计一套基于 FreeSWITCH \+ vibevoice\-asr \+ vibevoice\-tts \+ qwen3\.6 的本地化智能外呼系统。系统需要同时满足 **客服、催收、营销** 三种业务使用，必须在TTS配置层面做到严格业务隔离，不同业务拥有独立音色、语速、语调、参数、日志、录音、话术。整套系统要求私有化部署、开源模型、无第三方公有云接口、适合生产商用。

## 二、硬性技术要求（全部约束条件）

1. **技术栈固定不可更改**：FreeSWITCH 作为呼叫核心、微软 Vibevoice 本地ASR、Vibevoice本地TTS、通义千问3\.6本地大模型。

2. **部署方式**：全部私有化本地部署，不使用阿里云、百度等公有云语音接口。

3. **通信协议**：ASR/TTS 必须封装为MRCPv2协议对接FreeSWITCH，使用UniMRCP中间层。

4. **业务隔离要求（核心）**：
        

    - 客服业务：温柔、慢速、平稳女声；

    - 催收业务：严肃、稳重、有力男声；

    - 营销业务：热情、轻快、活力女声；

    - 三种业务TTS发音人、语速、音量、音调完全独立隔离；

    - 拨号计划隔离、录音目录隔离、日志隔离、大模型提示词隔离、并发上限隔离。

5. **通话流程**：FreeSWITCH通话流 → 实时语音送VibeVoice ASR转文字 → 文字送入通义千问3\.6推理 → 返回应答文本 → 按当前业务类型调用对应音色VibeVoice TTS合成语音 → 返还给通话通道播放。

6. **脚本要求**：提供可生产使用的Python AI交互脚本，嵌入FreeSWITCH，支持循环对话、静默检测、自动挂断、开场白区分业务。

7. **安装要求**：给出VibeVoice ASR、VibeVoice TTS详细编译安装步骤、UniMRCP编译部署、FreeSWITCH模块配置、MRCP对接完整流程。

8. **系统环境**：Linux CentOS7 / Ubuntu22\.04，GPU推理，CUDA加速。

9. **生产要求**：包含开机自启、守护进程、并发限制、录音分类存储、故障重试、日志切割。

## 三、需要AI输出的内容格式（固定输出结构）

请严格按照以下结构输出，不要多余话术、不要简化：

1. 系统整体架构图（文字拓扑）；

2. 服务器硬件配置推荐；

3. VibeVoice ASR\+TTS 完整安装步骤；

4. UniMRCP 编译安装\+MRCPv2配置；

5. FreeSWITCH全部配置文件（拨号计划、MRCP配置、模块加载）；

6. 三大业务TTS隔离详细参数表（音色、语速、音量、音调）；

7. Python AI对话完整源码（对接Qwen3\.6\+ASR\+TTS）；

8. 生产级systemd守护进程配置；

9. 开机自启、目录规划、日志隔离、录音隔离方案；

10. 常见报错、坑点、优化调优方案。

## 四、禁止事项

- 禁止推荐公有云语音接口；

- 禁止更换模型、禁止使用其他ASR/TTS；

- 禁止删减业务隔离逻辑；

- 禁止使用简单http接口直连跳过MRCP协议；

- 禁止输出测试版、残缺版代码。

> （注：文档部分内容可能由 AI 生成）
